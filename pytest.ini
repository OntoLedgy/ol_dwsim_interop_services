[pytest]
testpaths = mcp_service/server/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: marks tests as integration (may require DWSIM worker); deselect with -m "not integration"
